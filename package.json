{
  "name": "pawmarker",
  "version": "1.0.0",
  "description": "Image carousel app with Electron and Android support",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/app\" \"npm run dev -w packages/desktop\"",
    "dev:app": "npm run dev -w packages/app",
    "dev:desktop": "npm run dev -w packages/desktop",
    "build": "npm run build -w packages/app && npm run build -w packages/desktop",
    "build:app": "npm run build -w packages/app",
    "build:desktop": "npm run build -w packages/desktop",
    "android:prepare": "npm run build:app && npm run prepare -w packages/mobile",
    "android:build": "npm run build -w packages/mobile"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
